module.exports = {
  env: {
    API_DOMAIN: process.env.API_DOMAIN,
  },
  webpack: (config, { isServer }) => {
    // Fixes npm packages that depend on `fs` module
    if (!isServer) {
      config.node = {
        fs: 'empty'
      }
    }

    return config
  },
};
